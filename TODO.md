# TODO

- Implement preset profiles into oci launch command
    - How to handle profile selection? (defaults, named args)
- Implement runtime named args for overriding default instance params
    - Use drupal.org bash implementation 
    - Create + append to query string after parsing named args
- Gracefully handle "other" type response errors